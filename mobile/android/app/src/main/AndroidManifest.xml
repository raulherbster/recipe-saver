<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Recipe Saver"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Main launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Share intent for receiving text/URLs -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>

            <!-- Deep links for YouTube URLs -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="www.youtube.com" android:pathPrefix="/watch"/>
                <data android:scheme="https" android:host="youtube.com" android:pathPrefix="/watch"/>
                <data android:scheme="https" android:host="youtu.be"/>
                <data android:scheme="https" android:host="m.youtube.com" android:pathPrefix="/watch"/>
                <data android:scheme="https" android:host="youtube.com" android:pathPrefix="/shorts"/>
                <data android:scheme="https" android:host="www.youtube.com" android:pathPrefix="/shorts"/>
            </intent-filter>

            <!-- Deep links for Instagram URLs -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="www.instagram.com" android:pathPrefix="/p/"/>
                <data android:scheme="https" android:host="instagram.com" android:pathPrefix="/p/"/>
                <data android:scheme="https" android:host="www.instagram.com" android:pathPrefix="/reel/"/>
                <data android:scheme="https" android:host="instagram.com" android:pathPrefix="/reel/"/>
                <data android:scheme="https" android:host="www.instagram.com" android:pathPrefix="/reels/"/>
                <data android:scheme="https" android:host="instagram.com" android:pathPrefix="/reels/"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
